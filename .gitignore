# Raw Spotify export files: large, contain PII (ip_addr), and are only
# needed locally to run `npm run build`. Kept in data/ specifically so
# this one directory pattern is the only thing that has to stay correct.
# stats.json (committed) is what the site actually serves.
/data/

node_modules/
.vercel/
