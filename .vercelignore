# Raw Spotify export files: contain ip_addr and are large. Never upload
# them to Vercel, even data1.json which is already tracked in git.
# stats.json (committed, generated by `npm run build`) is what ships.
data*.json

node_modules
build-stats.js
